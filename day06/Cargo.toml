[package]
name = "day06"
edition = "2024"

[dependencies]
common = { path = "../common" }
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }

[features]
part2 = []
default = ["part2"]
instrumented = ["part2", "dep:serde", "dep:serde_json"]
